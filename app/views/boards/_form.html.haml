
=form_for ([@board, @board.nodes.build]) do |f|
  .field
    = f.label :head
    = f.text_field :head
  .field
    = f.label :body
    = f.text_area :body
  .field
    = f.label :picture
    = f.file_field :picture
    = f.hidden_field :picture_cache
  .actions
    =f.submit 'Save'                                      
